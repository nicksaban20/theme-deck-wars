# Anthropic API Key for Claude card generation
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# PartyKit Host (for WebSocket connections)
# Local development: localhost:1999
# Production: theme-deck-wars.your-username.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=theme-deck-wars.nicksaban20.partykit.dev

# Next.js App URL (for PartyKit to call API routes)
# Local development: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cloudflare Workers AI (for image generation)
# Get credentials at: https://dash.cloudflare.com
# Account ID: Found in dashboard URL
# API Token: Profile > API Tokens > Create Token > "Workers AI" template
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# ============ DATABASE (Optional but recommended) ============
# Vercel Postgres - Enables theme caching and game history
# Create at: https://vercel.com/dashboard -> Storage -> Create Database -> Postgres
# This is automatically added when you link Vercel Postgres to your project
POSTGRES_URL=postgres://...

# ============ IMAGE STORAGE (Optional but recommended) ============
# Vercel Blob - Persistent image storage
# Create at: https://vercel.com/dashboard -> Storage -> Create Database -> Blob
# This is automatically added when you link Vercel Blob to your project
BLOB_READ_WRITE_TOKEN=vercel_blob_...

# ============ LOCAL DIFFUSION (Optional) ============
# Use a local Stable Diffusion instance instead of Cloudflare

# Option 1: AUTOMATIC1111 WebUI (requires --api flag)
LOCAL_DIFFUSION_ENABLED=false
LOCAL_DIFFUSION_URL=http://127.0.0.1:7860

# Option 2: GGUF via sd-api-server (WasmEdge) - Select "Local AI" in game
# Download model: https://huggingface.co/second-state/stable-diffusion-v1-5-GGUF
# Start: wasmedge sd-api-server.wasm --model stable-diffusion-v1-5-pruned-emaonly-Q4_0.gguf
LOCAL_GGUF_URL=http://127.0.0.1:8080
